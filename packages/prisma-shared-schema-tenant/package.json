{
  "name": "@repo/prisma-shared-schema-tenant",
  "version": "1.0.0",
  "description": "Shared prisma schema for tenant",
  "main": "index.js",
  "scripts": {
    "db:generate": "npm run prisma generate",
    "db:migrate": "npm run prisma migrate dev --skip-generate",
    "db:deploy": "npm run prisma migrate deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "6.5.0"
  },
  "devDependencies": {
    "prisma": "6.5.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}